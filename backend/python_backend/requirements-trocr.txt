# Local runtime for backend/python_backend/trocr_worker.py
# PyTorch CPU wheel selection is platform-specific; install it before these.
transformers>=5.0.0
torch>=2.0.0
pillow>=10.0.0
safetensors>=0.4.0
